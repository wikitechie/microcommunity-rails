#wikipage.well
	= button_to "Edit", edit_wikipage_path(@wikipage), :method => "get", :class => 'btn btn-success pull-right', :remote => true, :id => "wikipage-edit-button"
	.page-header
		%h1= wikipage.title
	#wikipage-body
		%p= wikipage.body

